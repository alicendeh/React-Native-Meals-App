import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: 60,
    height: 60,
    borderRadius: 60,
  },
  img: {
    width: '100%',
    height: '100%',
    borderRadius: 50,
  },
});

export default styles;
